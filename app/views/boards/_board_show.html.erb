<div class="card">
  <div class="card-header">
    <h4><%= unko.title %></h4>
    <% unko.tags.each do |tag| %>
    <span class="badge badge-primary"><%= tag.name %></span>
    <% end %>
  </div>
  <div class="card-body">
    <p class="card-text"><%= simple_format(unko.body) %></p>
    <p class="text-right font-weight-bold mr-10"><%= unko.name %></p>
  </div>
</div>