- if current_page.last?
  = tag.a raw(t('views.pagination.next')),
    class: 'page-link next disabled'
- else
  = link_to raw(t('views.pagination.next')),
    url,
    rel: 'next',
    remote: remote,
    class: 'page-link next'
